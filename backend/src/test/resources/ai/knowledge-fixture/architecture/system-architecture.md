# System Architecture

## Overview

One Spring Boot backend and one React frontend share a MySQL database.

## Components

The scheduler runs inside the backend process, and the timeline is read from the
same database.